import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.saapp',
  appName: 'saapp',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
